spring:
  profiles: integrationtest
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=PostgreSQL

---

spring:
  profiles: functionaltest
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=PostgreSQL
